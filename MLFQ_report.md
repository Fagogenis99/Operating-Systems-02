# Αναφορά Εργασίας: Υλοποίηση MLFQ Scheduler στο xv6

**Konstantinos Fagogenis** - sdi2100199

## 1. Λίστα Τροποποιημένων Αρχείων

Η λίστα με τα αρχεία που τροποποιήθηκαν/δημιουργήθηκαν για τις ανάγκες της εργασίας:

### Αρχεία Πυρήνα (Kernel)
* **`kernel/proc.h`**: Προστέθηκαν στην δομή `struct proc`  τα πεδία `priority`, `ticks_used` (ticks που καταναλώθηκαν στο τρέχον επίπεδο) και `ticks_waiting` (ticks αναμονής).
* **`kernel/pstat.h`**(new): Δημιουργήθηκε για τον ορισμό της δομής `struct pstat`, η οποία χρησιμοποιείται για την ανταλλαγή δεδομένων διεργασιών μεταξύ πυρήνα και χρήστη.
* **`kernel/proc.c`**:
    * `scheduler()`: Υλοποιήθηκε ο MLFQ scheduler.
    * `allocproc()`: Αρχικοποίηση των νέων πεδίων (`priority = 0`, κτλ.) κατά τη δημιουργία διεργασίας.
    * `update_time()`: Προστέθηκε νέα συνάρτηση για την υλοποίηση του μηχανισμού anti-starvation.
* **`kernel/trap.c`**: Τροποποιήθηκαν οι `usertrap()` και `kerneltrap()` ώστε σε κάθε timer interrupt να καλούνται:
    * Η `check_time()` για τον έλεγχο εξάντλησης του χρονομεριδίου (Demotion).
    * Η `update_time()` για τον έλεγχο λιμοκτονίας (Starvation).
* **`kernel/syscall.h`**: Προστέθηκε ο αριθμός της νέας κλήσης συστήματος `SYS_getpinfo`.
* **`kernel/syscall.c`**: Καταχωρήθηκε η νέα κλήση συστήματος στον πίνακα συναρτήσεων.
* **`kernel/sysproc.c`**: Υλοποιήθηκε η συνάρτηση `sys_getpinfo()`, η οποία αντιγράφει τα στοιχεία των διεργασιών από το `struct proc` στο `struct pstat`.

### Αρχεία Χρήστη (User)
* **`user/user.h`**: Προστέθηκε η δήλωση της κλήσης συστήματος `getpinfo()` και της δομής `struct pstat` ώστε να είναι ορατά στα προγράμματα χρήστη.
* **`user/usys.pl`**: Προστέθηκε η εγγραφή `getpinfo` για την παραγωγή του assembly stub της κλήσης συστήματος.
* **`user/ps.c`** (new): Υλοποιήθηκε το πρόγραμμα χρήστη `ps` που καλεί την `getpinfo` και τυπώνει τις τρέχουσες διεργασίες, τις προτεραιότητες, το μέγεθος και άλλα στατιστικά στοιχεία.

### Makefile
* **`Makefile`**: Έγιναν μικρές προσαρμογές για τη διασφάλιση συμβατότητας με την έκδοση του QEMU.

---

## 2. Περιγραφή Υλοποίησης

Η υλοποίηση του **MLFQ** βασίστηκε σε τέσσερις ουρές προτεραιότητας (0 έως 3), όπου το 0 αντιπροσωπεύει την υψηλότερη προτεραιότητα και το 3 τη χαμηλότερη.

**Scheduler:**
Η συνάρτηση `scheduler` σαρώνει τις ουρές ξεκινώντας πάντα από την **Προτεραιότητα 0**. Αν βρεθεί διεργασία σε κατάσταση `RUNNABLE`, εκτελείται άμεσα. Μόνο αν δεν υπάρχουν έτοιμες διεργασίες σε υψηλότερο επίπεδο, ο scheduler εξετάζει τα χαμηλότερα επίπεδα. Εντός του ίδιου επιπέδου, οι διεργασίες εξυπηρετούνται με μέθοδο Round-Robin (σαρώνοντας τον πίνακα `proc`). Αν βρεθεί οποιαδήποτε διεργασία προς εκτέλεση, η σάρωση επανεκκινεί από το επίπεδο 0 για να διασφαλιστεί η προτεραιότητα.

**Μηχανισμός Υποβιβασμού (Demotion):**
Στο αρχείο `trap.c`, κάθε φορά που συμβαίνει ένα timer interrupt (tick), αυξάνουμε τον μετρητή `ticks_used` της τρέχουσας διεργασίας. Αν ο μετρητής ξεπεράσει το όριο του επιπέδου της (4, 8, 16, 32 ticks αντίστοιχα), η διεργασία υποβιβάζεται στο αμέσως κατώτερο επίπεδο (π.χ. από 0 σε 1) και τα ticks της μηδενίζονται.

**Μηχανισμός Anti-Starvation:**
Για την αποφυγή λιμοκτονίας των διεργασιών χαμηλής προτεραιότητας, υλοποιήθηκε η συνάρτηση `update_time`. Σε κάθε tick, αυξάνεται ο χρόνος αναμονής (`ticks_waiting`) για όλες τις `RUNNABLE` διεργασίες. Αν μια διεργασία περιμένει περισσότερο από το δεκαπλάσιο του χρονομεριδίου της (10x limit του τρέχοντος επιπέδου), προάγεται σε υψηλότερο επίπεδο προτεραιότητας (π.χ. από 3 σε 2), εξασφαλίζοντας ότι τελικά θα εκτελεστεί.